import 'package:flutter/material.dart';

class AdsManager extends StatelessWidget {
  const AdsManager({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
