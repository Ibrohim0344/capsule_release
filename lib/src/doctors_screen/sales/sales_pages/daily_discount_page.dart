import 'package:flutter/material.dart';

class DailyDiscountPage extends StatelessWidget {
  const DailyDiscountPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
